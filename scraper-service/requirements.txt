fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0

# Scrapling — adaptive web scraping with anti-bot bypass
# [fetchers] pulls in curl_cffi for TLS-impersonating HTTP fetches
scrapling[fetchers]>=0.3.0

# For discovering LinkedIn post URLs via DuckDuckGo HTML search
beautifulsoup4>=4.12.0
lxml>=5.0.0
